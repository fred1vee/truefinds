# Server-only PostgreSQL connection used by Prisma.
DATABASE_URL=""

# Better Auth server configuration.
# Generate a high-entropy secret with: npx auth secret
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="http://localhost:3000"

# Server-only tracking provider configuration.
# Leave empty until a real tracking adapter is approved and implemented.
TRACKING_API_BASE_URL=
TRACKING_API_KEY=
